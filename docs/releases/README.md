# Release Notes

This directory keeps the reviewed GitHub Release notes for published desktop versions.

The GitHub Release body should match the corresponding `vX.Y.Z.md` file. Use these files as the source when editing a release with `gh release edit --notes-file`.
